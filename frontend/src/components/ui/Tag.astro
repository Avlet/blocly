---
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---

<span
  class:list={[
    "inline-flex items-center border border-line px-3 py-1.5 font-mono text-[0.62rem] uppercase tracking-[0.12em] text-fg-subtle",
    className,
  ]}
>
  <slot />
</span>