{
  "name": "blocly",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "dev:web": "astro dev",
    "dev:api": "cd backend && python -m uvicorn app.main:app --reload --port 8000",
    "build": "astro build"
  },
  "dependencies": {
    "astro": "^6.4.8",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "overrides": {
    "vite": "^7.0.0"
  }
}